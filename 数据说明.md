# 数据说明

## 1. 数据总体说明

本项目的数据主要用于构建脐橙知识库，支撑本地 RAG 检索问答。数据包括原始资料、清洗后的知识库文本、切分后的 chunk、Chroma 向量库和 SQLite 业务数据库。

数据大致流程：

```text
原始 PDF/docx
→ 文本抽取或人工清洗
→ Markdown/Word 清洗稿
→ JSON chunk
→ Chroma 向量库
→ 后端 RAG 检索问答
```

## 2. 原始资料目录

```text
docs/
```

该目录保存原始资料，主要包括：

- 脐橙栽培技术资料。
- 病虫害防治资料。
- 赣南脐橙、纽荷尔脐橙等相关论文。
- 国家标准、地方标准、行业规程。
- 少量 docx 示例文件。

这些文件是知识库的原始来源，建议交接时保留。

## 3. 清洗后资料目录

```text
docs_clean/
```

该目录保存清洗和整理后的知识库资料。

主要文件和目录：

```text
docs_clean/markdown/
```

`docs_clean/README.md` 中记录了早期文档清洗结果：源文件来自 `docs`，曾生成 73 个 Markdown，部分扫描版 PDF 通过 OCR 转成结构化 Markdown。

## 4. 知识库 chunk 目录

```text
backend/chunks/
```

该目录保存准备写入向量库的 JSON chunk 文件。每个 JSON 文件通常对应一个 Markdown 文件，里面包含切分后的文本块和元数据。

chunk 中常见字段：

```text
chunk_id        文本块编号
source_file     来源文件
h1/h2/h3         标题层级
title_path      标题路径
content         正文内容
```

这些 chunk 由 `backend/document_processor.py` 生成，后续可以手动修改后再写入向量库。

## 5. 向量库目录

```text
backend/chroma_db/
```

该目录是 ChromaDB 向量库，保存本地知识库的向量数据。后端 RAG 检索会读取该目录。

目录中常见内容：

```text
backend/chroma_db/chroma.sqlite3
backend/chroma_db/<uuid目录>/
```

如果交接时保留这个目录，同学可以直接使用已有知识库。如果删除该目录，需要重新运行 `backend/document_processor.py` 构建向量库。

注意：重新构建向量库需要可用的 `DASHSCOPE_API_KEY`，因为项目使用 DashScope Embedding。

## 6. SQLite 业务数据库

```text
backend/orange_agent.db
```

这是项目的业务数据库，使用 SQLite，不需要额外安装数据库服务器。

主要保存：

- 用户账号。
- 会话列表。
- 聊天消息。
- 图片消息对应的 base64 数据。

数据库表结构定义在：

```text
backend/database.py
```

主要表：

```text
users       用户表
sessions    会话表
messages    消息表
```

如果不希望交接历史用户和聊天记录，可以删除 `backend/orange_agent.db`。后端启动时会自动重新创建空数据库。

## 7. 重新构建知识库的大致流程

如果后续修改了 `docs_clean/markdown/` 中的资料，想重新生成向量库，可以参考下面流程。

1. 配置 `backend/.env` 中的 `DASHSCOPE_API_KEY`。
2. 确认 `backend/document_processor.py` 的输入目录为 `docs_clean/markdown`。
3. 先生成 JSON chunk。
4. 检查或人工修改 `backend/chunks/` 中的 JSON。
5. 再写入 `backend/chroma_db/`。

当前 `backend/document_processor.py` 中已经提供这些方法：

```text
generate_chunks_from_markdown_directory()
build_vectorstore_from_chunk_directory()
add_chunks_from_markdown()
add_documents_to_vectorstore()
```

注意：

- `build_vectorstore_from_chunk_directory()` 会重建向量库。
- `add_documents_to_vectorstore()` 是增量添加，适合只新增少量资料。
- 重建或增量写入前，最好先备份 `backend/chroma_db/`。

## 8. 交接时建议保留的数据

建议保留：

```text
docs/
docs_clean/
backend/chunks/
backend/chroma_db/
backend/orange_agent.db
```

如果压缩包太大，优先保留：

```text
docs_clean/
backend/chunks/
backend/chroma_db/
```

其中 `docs/` 是原始资料，体积可能较大，但最好另行打包备份。

## 9. 数据安全提醒

1. 真实 API Key 不要写进文档、压缩包或 Git 仓库。
2. 如果 `backend/orange_agent.db` 中包含真实用户聊天记录，交接前应确认是否可以保留。
3. 如果聊天记录中包含图片 base64，数据库文件可能变大，也可能包含用户上传图片内容。
4. 对外展示或公开仓库时，建议只保留示例数据库或空数据库。
