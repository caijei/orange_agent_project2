# 项目交接说明

## 1. 项目简介

本项目是本科毕设项目：基于 Agent 的脐橙知识问答系统。系统面向脐橙种植、病虫害防治、品质评价、栽培管理等场景，支持用户通过网页聊天界面进行文本问答，也支持上传图片让模型进行辅助分析。

系统主要能力：

- 基于本地脐橙知识库进行 RAG 检索问答。
- 调用大模型完成问答、任务路由和多轮对话。
- 支持联网搜索补充最新信息。
- 支持图片上传和多模态分析。
- 保存用户、会话和聊天记录。

## 2. 技术栈

后端：

- Python
- FastAPI
- LangChain
- ChromaDB
- SQLite
- DashScope
- Tavily

前端：

- React
- Vite
- TypeScript
- Tailwind CSS

## 3. 主要目录

```text
orange_agent_project/
├── backend/              # 后端接口、Agent、RAG、数据库、向量库
├── frontend/             # 前端页面
├── docs/                 # 原始 PDF、docx 等资料
├── docs_clean/           # 清洗后的 Markdown、Word、汇总知识库
├── README.md             # 项目安装和运行说明
├── 项目交接说明.md        # 本文件
└── 数据说明.md            # 数据、数据库和知识库说明
```

## 4. 运行方式

完整安装和启动步骤见 `README.md`。简要流程如下。

后端安装依赖：

```powershell
python -m pip install -U pip
python -m pip install -r backend\requirements.txt
copy backend\.env.example backend\.env
```

后端启动服务：

```powershell
cd backend
python api_server.py
```

这些命令会直接使用系统 Python 环境，不在项目里创建 `.venv`。启动后端前，需要在 `backend/.env` 中填写自己的 `DASHSCOPE_API_KEY`。如果需要联网搜索，还需要填写 `TAVILY_API_KEY`。

前端安装依赖：

```powershell
cd frontend
npm ci
```

前端启动项目：

```powershell
npm run dev
```

浏览器访问：

```text
http://localhost:5173
```

后端默认地址：

```text
http://localhost:8888
```

## 5. 后端关键文件

```text
backend/api_server.py
```

FastAPI 后端入口，负责注册、登录、会话管理、聊天接口、SSE 流式输出等。

```text
backend/agent_service.py
```

Agent 主逻辑，负责判断用户问题类型、调用本地知识库、联网搜索、多模态模型等。

```text
backend/rag_service.py
```

RAG 检索服务，负责向量库检索、重排、知识库问答等。

```text
backend/document_processor.py
```

文档处理和向量库构建脚本。可以把清洗后的 Markdown 切分成 chunk，再写入 ChromaDB。

```text
backend/database.py
```

SQLite 数据库层，负责用户、会话、消息的增删查改。

```text
backend/orange_agent.db
```

SQLite 数据库文件，保存用户、会话和聊天记录。

```text
backend/chroma_db/
```

Chroma 向量库目录，保存本地知识库的向量数据。

## 6. 前端关键文件

```text
frontend/src/ChatApp.tsx
```

前端主聊天页面，包含登录注册、会话列表、消息展示、图片上传、检索模式切换、流式回答展示等。

```text
frontend/package.json
```

前端依赖和启动脚本。

## 7. 数据和知识库

数据说明见 `数据说明.md`。核心数据目录如下：

- `docs/`：原始 PDF、docx 等资料。
- `docs_clean/`：清洗后的 Markdown、Word 和知识库汇总文件。
- `backend/chunks/`：用于入向量库的 JSON chunk。
- `backend/chroma_db/`：Chroma 向量库。
- `backend/orange_agent.db`：SQLite 业务数据库。

## 8. 交接时建议保留

建议交给接手同学的内容：

```text
backend/
frontend/
docs/
docs_clean/
README.md
项目交接说明.md
数据说明.md
```

其中 `backend/chroma_db/`、`backend/chunks/`、`backend/orange_agent.db` 如果希望同学拿到后直接使用已有知识库和聊天数据库，可以一起保留。

8. 

## 9. 给接手同学的建议阅读顺序

建议按下面顺序看：

1. 先看 `README.md`，把项目跑起来。
2. 再看 `项目交接说明.md`，了解整体结构。
3. 再看 `数据说明.md`，了解数据和知识库来源。
4. 后端从 `backend/api_server.py` 开始看。
5. Agent 和 RAG 从 `backend/agent_service.py`、`backend/rag_service.py` 开始看。
6. 前端从 `frontend/src/ChatApp.tsx` 开始看。
